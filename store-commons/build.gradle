buildscript {
    ext {
        excludedFromCoverage = []
    }
}

plugins {
    id 'common-setup-conventions'
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    api "org.imaginarystore.libraries:store-models:${storeModelsVersion}"
}


test {
    useJUnitPlatform()
}